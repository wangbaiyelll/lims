<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.library.mapper.MessageMapper">

    <resultMap id="BaseResultMap" type="com.example.library.entity.Message">
        <id column="id" property="id"/>
        <result column="teacher_id" property="teacherId"/>
        <result column="type" property="type"/>
        <result column="title" property="title"/>
        <result column="content" property="content"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="read_time" property="readTime"/>
    </resultMap>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM message WHERE id = #{id}
    </select>

    <select id="selectByTeacherId" resultMap="BaseResultMap">
        SELECT * FROM message
        WHERE teacher_id = #{teacherId}
        <if test="status != null">
            AND status = #{status}
        </if>
        ORDER BY create_time DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="countByTeacherId" resultType="int">
        SELECT COUNT(*) FROM message
        WHERE teacher_id = #{teacherId}
        <if test="status != null">
            AND status = #{status}
        </if>
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO message (teacher_id, type, title, content, status, create_time)
        VALUES (#{teacherId}, #{type}, #{title}, #{content}, 0, NOW())
    </insert>

    <update id="updateStatus">
        UPDATE message
        SET status = #{status}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM message WHERE id = #{id}
    </delete>

</mapper>